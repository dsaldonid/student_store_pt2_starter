const db = require("../db")

class Store{
    static async listProducts(){
        //grab all products from our database
        //That method should run a SQL query that 
        //searches the database for all products and returns a list of them.
    }
}

module.exports = Store